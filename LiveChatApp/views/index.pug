extends layout

block content
  div(id="chat")
    div(id="chat-window")
      div(id="output")
      div(id="feedback")
    input(id="person" type="text" placeholder="Name")
    input(id="message" type="text" placeholder="Message")
    button(id="send") Send
